// [MOVIDO — FUERA DE ALCANCE v1.0]
// El log de operación ahora vive en memoria en el service worker (ver
// src/lib/log.ts — CircularLog) y se serializa al ZIP como `log.txt`.
// La v1.0 no persiste un log de auditoría entre corridas.
export {};

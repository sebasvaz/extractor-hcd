import { describe, it } from 'vitest';

// [FUERA DE ALCANCE v1.0] — tests del parser CDA se mueven al pipeline server-side.
describe.skip('CDA parser (movido al pipeline server-side)', () => {
  it('placeholder', () => {});
});
